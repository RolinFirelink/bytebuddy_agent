# ByteBuddy Agent 快速开始指南

## 🚀 快速测试

### 1. 编译项目
```bash
mvn clean package
```

### 2. 运行测试（选择一种模式）

#### 默认模式（拦截 Controller）
```bash
java -javaagent:target/bytebuddy_agent-1.0-SNAPSHOT.jar -cp target/classes org.example.TestApplication
```

#### 包路径匹配模式
```bash
java -javaagent:target/bytebuddy_agent-1.0-SNAPSHOT.jar=package -cp target/classes org.example.TestApplication
```

#### Advice 模式（性能更好）
```bash
java -javaagent:target/bytebuddy_agent-1.0-SNAPSHOT.jar=advice -cp target/classes org.example.TestApplication
```

#### 高级拦截器模式（捕获更多信息）
```bash
java -javaagent:target/bytebuddy_agent-1.0-SNAPSHOT.jar=advanced -cp target/classes org.example.TestApplication
```

#### 所有模式
```bash
java -javaagent:target/bytebuddy_agent-1.0-SNAPSHOT.jar=all -cp target/classes org.example.TestApplication
```

## 📝 新增文件清单

### 拦截器
- ✅ `src/main/java/org/example/agent/interceptor/PackageMatcherInterceptor.java`
- ✅ `src/main/java/org/example/agent/interceptor/AdviceInterceptor.java`
- ✅ `src/main/java/org/example/agent/interceptor/AdvancedInterceptor.java`

### 匹配器
- ✅ `src/main/java/org/example/agent/matcher/CustomMatchers.java`

### 监听器
- ✅ `src/main/java/org/example/agent/listener/ClassLoadListener.java`

### 测试类
- ✅ `src/main/java/org/example/service/UserService.java`
- ✅ `src/main/java/org/example/service/OrderService.java`
- ✅ `src/main/java/org/example/dao/UserDao.java`
- ✅ `src/main/java/org/example/TestApplication.java`

### 文档
- ✅ `ByteBuddy功能扩展说明文档.md` - 详细文档
- ✅ `快速开始指南.md` - 本文件

## 🔍 功能对比

| 模式 | 拦截器 | 特点 | 适用场景 |
|------|--------|------|----------|
| default | ControllerInterceptor | 拦截 Controller 注解 | Spring Web 应用 |
| package | PackageMatcherInterceptor | 包路径匹配 | 特定包下的类 |
| advice | AdviceInterceptor | 高性能拦截 | 需要高性能的场景 |
| advanced | AdvancedInterceptor | 详细信息捕获 | 调试和监控 |
| all | 所有拦截器 | 多种拦截方式 | 演示和学习 |

## 📚 详细文档

查看 `ByteBuddy功能扩展说明文档.md` 获取完整说明。

